# Tron 钱包架构说明

## 📋 钱包架构概览

系统中有**两种类型的钱包**：

1. **用户充值钱包**（HD 钱包管理）
2. **平台热钱包**（独立钱包）

---

## 1. 用户充值钱包（HD 钱包）

### 架构说明

**是的，使用 HD 钱包逻辑管理！**

- ✅ **每个用户有独立的私钥和地址**
- ✅ **所有用户地址都从一个主种子（master seed）派生**
- ✅ **每个用户的私钥都不同**

### 工作流程

```
主种子 (Master Seed)
    ↓
HD 钱包派生 (BIP44: m/44'/195'/0'/0/{index})
    ↓
用户 1 → 地址 A (私钥 A, index=0)
用户 2 → 地址 B (私钥 B, index=1)
用户 3 → 地址 C (私钥 C, index=2)
...
```

### 特点

1. **统一管理**：
   - 所有用户地址都从一个主种子派生
   - 只需备份主种子，即可恢复所有地址

2. **独立私钥**：
   - 每个用户有自己独立的私钥
   - 私钥加密存储在 `user_tron_wallets` 表

3. **用途**：
   - **仅用于接收充值**
   - 用户向这个地址发送 USDT
   - 系统扫描到这个充值后，会归集到热钱包

### 数据存储

**表：`user_tron_wallets`**
- `user_id` - 用户ID
- `tron_address` - 用户充值地址（每个用户不同）
- `derivation_index` - 派生索引（HD钱包）
- `encrypted_private_key` - 加密后的私钥（每个用户不同）

**表：`tron_hd_wallet`**
- `encrypted_master_seed` - 加密后的主种子（唯一）
- `next_derivation_index` - 下一个派生索引

---

## 2. 平台热钱包

### 配置

```env
TRON_HOT_WALLET_ADDRESS=Txxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TRON_HOT_WALLET_PRIVATE_KEY=your_hot_wallet_private_key_hex
```

### 用途

**热钱包是平台自己的钱包，与用户钱包完全独立！**

#### 用途 1: 归集（Sweep）

**流程：**
```
用户充值地址 (用户1) → [归集] → 热钱包
用户充值地址 (用户2) → [归集] → 热钱包
用户充值地址 (用户3) → [归集] → 热钱包
```

**说明：**
- 定时任务扫描所有用户充值地址
- 如果余额 >= 阈值（默认 50 USDT），执行归集
- 使用**用户地址的私钥**发送 USDT 到**热钱包地址**
- 目的是集中管理资金，降低风险

**代码位置：** `TronSweepService::sweepAddress()`

```php
// 使用用户地址的私钥
$privateKey = $this->walletService->decryptPrivateKey($wallet->encrypted_private_key);

// 发送到热钱包
$this->usdtContract->transferFromPrivateKey(
    $privateKey,                    // 用户地址的私钥
    $this->hotWalletAddress,        // 热钱包地址（目标）
    $usdtBalance
);
```

#### 用途 2: 提币（Withdrawal）

**流程：**
```
热钱包 → [提币] → 用户指定的外部地址
```

**说明：**
- 用户申请提币时，从热钱包发送 USDT
- 使用**热钱包的私钥**发送交易
- 发送到用户指定的外部地址

**代码位置：** `TronWithdrawalService::sendFromHotWallet()`

```php
// 使用热钱包的私钥
$privateKey = $this->decryptHotWalletKey($this->hotWalletPrivateKey);

// 发送到用户指定的地址
$this->usdtContract->transferFromPrivateKey(
    $privateKey,        // 热钱包的私钥
    $toAddress,         // 用户指定的外部地址
    $amount
);
```

### 特点

- ✅ **独立钱包**：与用户钱包完全分开
- ✅ **平台所有**：属于平台，不是用户的钱包
- ✅ **集中管理**：所有归集的资金都在这里
- ✅ **提币来源**：所有提币都从这里发出

---

## 🔄 完整资金流转

### 充值流程

```
外部地址
    ↓ (发送 USDT)
用户充值地址 (HD派生，用户1)
    ↓ (扫描到充值)
系统记录充值
    ↓ (确认数达到阈值)
用户账户余额增加
    ↓ (定时归集，余额 >= 50 USDT)
热钱包 (集中管理)
```

### 提币流程

```
用户账户余额
    ↓ (申请提币)
冻结余额
    ↓ (管理员审核通过)
热钱包
    ↓ (使用热钱包私钥发送)
用户指定的外部地址
```

---

## 📊 钱包对比

| 特性 | 用户充值钱包（HD） | 平台热钱包 |
|------|-------------------|-----------|
| **数量** | 每个用户一个 | 平台一个（共享） |
| **管理方式** | HD 钱包派生 | 独立钱包 |
| **私钥来源** | 从主种子派生 | 独立生成 |
| **用途** | 接收充值 | 归集、提币 |
| **资金流向** | 接收 → 归集到热钱包 | 接收归集 → 发送提币 |
| **用户可见** | ✅ 用户可以看到自己的充值地址 | ❌ 用户看不到 |

---

## 🔐 安全说明

### 用户钱包安全

- ✅ 每个用户私钥独立加密存储
- ✅ 从主种子派生，统一管理
- ✅ 私钥不会泄露给用户

### 热钱包安全

- ⚠️ **非常重要**：热钱包私钥必须安全保管
- ⚠️ 建议使用硬件钱包或多签钱包
- ⚠️ 限制热钱包余额，大额资金转移到冷钱包

---

## 📝 总结

### 问题 1: 用户钱包是 HD 钱包管理的吗？

**答案：是的！**

- ✅ 如果 HD 钱包已初始化，每个用户地址都从主种子派生
- ✅ 每个用户有独立的私钥和地址
- ✅ 所有用户地址都来自同一个主种子

**示例：**
- 用户1：地址 A，私钥 A（从主种子 index=0 派生）
- 用户2：地址 B，私钥 B（从主种子 index=1 派生）
- 用户3：地址 C，私钥 C（从主种子 index=2 派生）

### 问题 2: 热钱包是做什么的？跟用户钱包的关系？

**答案：热钱包是平台的钱包，与用户钱包完全独立！**

**用途：**
1. **归集**：收集用户充值地址的 USDT 到热钱包
2. **提币**：从热钱包发送 USDT 给用户

**关系：**
- 用户钱包：每个用户一个，用于接收充值
- 热钱包：平台一个，用于归集和提币
- **它们是分开的**，用户钱包的资金会归集到热钱包

**资金流向：**
```
用户充值 → 用户充值地址 → [归集] → 热钱包 → [提币] → 外部地址
```

---

## 🎯 关键点

1. **用户钱包 = HD 钱包派生**：每个用户有独立私钥，但都来自主种子
2. **热钱包 = 平台钱包**：独立于用户钱包，用于归集和提币
3. **归集**：用户地址 → 热钱包（使用用户私钥）
4. **提币**：热钱包 → 外部地址（使用热钱包私钥）

