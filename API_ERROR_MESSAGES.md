# API 错误提示说明

## 跟单相关错误（FollowController）

### 1. 合约账户余额不足

**场景**：用户尝试跟单，但合约账户余额不足

**错误消息**：
- `合约账户余额不足，请先从币币账户划转到合约账户` - 当用户币币账户有余额时
- `合约账户余额不足，请先充值` - 当用户币币账户也没有余额时

**API 响应**：
```json
{
  "error": "合约账户余额不足，请先从币币账户划转到合约账户"
}
```

**HTTP 状态码**：400

**处理逻辑**：
- 检查合约账户余额是否足够（1% 总资产）
- 如果不足，检查币币账户是否有余额
- 如果有，提示用户从币币账户划转
- 如果没有，提示用户先充值

---

### 2. 其他跟单错误

**已处理的错误消息**：
- `该跟单码已被使用，每个跟单码只能使用一次` - 跟单码重复使用
- `账户余额不足` - 总资产为零
- `今日跟单额度已用完` - 配额已用完
- `您没有权限参与此类型的跟单窗口` - 权限不足
- `跟单码错误或者未生效，请跟管理员确认` - 其他错误（默认）

---

## 提现相关错误（WithdrawController）

### 1. 未结算订单限制

**场景**：用户有未结算的跟单订单时尝试提现

**错误消息**：
```
您有未结算的跟单订单，无法提现。请等待订单结算后再试
```

**API 响应**：
```json
{
  "error": "您有未结算的跟单订单，无法提现。请等待订单结算后再试"
}
```

**HTTP 状态码**：400

**处理逻辑**：
- 检查用户是否有状态为 `placed` 的跟单订单
- 如果有，拒绝提现请求
- 提示用户等待订单结算后再试

---

### 2. 币币账户余额不足

**场景**：用户尝试提现，但币币账户余额不足

**错误消息**：
```
币币账户余额不足
```

**API 响应**：
```json
{
  "error": "币币账户余额不足"
}
```

**HTTP 状态码**：400

**处理逻辑**：
- 只检查币币账户（spot）余额
- 不检查合约账户（contract）余额
- 提示用户币币账户余额不足

---

### 3. 其他提现错误

**已处理的错误消息**：
- `身份认证未完成` - KYC 未完成
- `提现地址未绑定` - 提现地址未设置
- `提现密码未设置` - 提现密码未设置
- `Withdrawal password not set` - 提现密码未设置（服务层）
- `Withdrawal password is required` - 提现密码未提供
- `Invalid withdrawal password` - 提现密码错误
- `Insufficient available balance` - 可用余额不足

---

## 账户划转相关错误（TransferController）

### 1. 账户类型错误

**场景**：用户尝试从币币账户划转到币币账户（相同账户类型）

**错误消息**：
```
Cannot transfer to same account type
```

**API 响应**：
```json
{
  "error": "Cannot transfer to same account type"
}
```

**HTTP 状态码**：400

---

### 2. 源账户不存在

**场景**：用户尝试从币币账户划转，但币币账户不存在

**错误消息**：
- `您没有 spot 账户，请先从 contract 账户转到 spot 账户` - 如果合约账户有余额
- `spot 账户不存在，请先充值` - 如果合约账户也没有余额

**API 响应**：
```json
{
  "error": "您没有 spot 账户，请先从 contract 账户转到 spot 账户"
}
```

**HTTP 状态码**：400

---

## 错误处理最佳实践

### 1. 错误消息设计原则

- ✅ **用户友好**：使用中文，清晰说明问题
- ✅ **可操作**：提供解决方案或下一步操作
- ✅ **一致性**：相同类型的错误使用相同的消息格式
- ✅ **安全性**：不泄露敏感信息（如账户余额、内部错误详情）

### 2. 错误分类

**客户端错误（400）**：
- 参数验证失败
- 业务逻辑错误（余额不足、权限不足等）
- 资源不存在

**服务器错误（500）**：
- 数据库错误
- 系统异常
- 第三方服务错误

### 3. 错误响应格式

**生产环境**：
```json
{
  "error": "用户友好的错误消息"
}
```

**开发环境**：
```json
{
  "error": "用户友好的错误消息",
  "debug": {
    "message": "原始错误消息",
    "error_class": "Exception类名",
    "file": "文件路径",
    "line": "行号"
  }
}
```

---

## 测试建议

### 1. 跟单错误测试

```bash
# 测试合约账户余额不足
# 1. 用户只有币币账户余额
# 2. 尝试跟单
# 预期：返回 "合约账户余额不足，请先从币币账户划转到合约账户"

# 测试未结算订单限制
# 1. 用户有未结算的跟单订单
# 2. 尝试提现
# 预期：返回 "您有未结算的跟单订单，无法提现。请等待订单结算后再试"
```

### 2. 提现错误测试

```bash
# 测试币币账户余额不足
# 1. 用户只有合约账户余额
# 2. 尝试提现
# 预期：返回 "币币账户余额不足"

# 测试未结算订单限制
# 1. 用户有未结算的跟单订单
# 2. 尝试提现
# 预期：返回 "您有未结算的跟单订单，无法提现。请等待订单结算后再试"
```

---

最后更新：2025-11-20

