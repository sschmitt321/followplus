# FollowPlus 快速开始指南

## 🚀 快速验证测试环境

### 方法 1: 使用 Artisan 命令（推荐）

```bash
# 运行所有测试
php artisan test

# 运行特定测试文件
php artisan test tests/Feature/AuthTest.php

# 运行测试套件
php artisan test --testsuite=Feature
```

### 方法 2: 使用测试脚本

```bash
# 运行所有测试
./scripts/test.sh

# 运行特定测试文件
./scripts/test.sh tests/Feature/AuthTest.php
```

### 方法 3: 直接使用 Pest

```bash
./vendor/bin/pest
```

## ✅ 验证步骤

### 1. 检查环境

```bash
# 检查 PHP 版本（需要 8.2+）
php --version

# 检查 Composer
composer --version

# 检查 Laravel
php artisan --version
```

### 2. 安装依赖（如果还未安装）

```bash
composer install
```

### 3. 运行测试

```bash
# 运行所有测试
php artisan test
```

### 4. 预期结果

你应该看到类似以下的输出：

```
PASS  Tests\Feature\AuditLogTest
PASS  Tests\Feature\KycTest
...

Tests:  XX passed (XX assertions)
Duration: X.XXs
```

## 📝 重要说明

### ✅ 测试环境特点

1. **无需配置数据库**: 测试使用 SQLite 内存数据库，自动创建和清理
2. **无需 .env 文件**: 测试环境使用 `phpunit.xml` 中的配置
3. **自动清理**: 每次测试后自动清理数据

### ⚙️ 测试配置位置

- **phpunit.xml**: 测试环境配置
- **tests/Pest.php**: Pest 测试框架配置
- **tests/TestCase.php**: 基础测试类

### 🔍 如果测试失败

1. **检查 JWT Secret**: 已在 `phpunit.xml` 中配置
2. **检查迁移文件**: 确保所有迁移文件正确
3. **查看详细错误**: 运行 `php artisan test --verbose`

## 🎯 下一步

测试环境验证通过后，可以：

1. ✅ 开始实现模块 2（Accounts & Ledger）
2. ✅ 为新功能添加测试用例
3. ✅ 运行持续集成测试

---

**提示**: 所有测试都在独立的内存数据库中运行，不会影响开发环境。

